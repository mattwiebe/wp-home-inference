# Repo Notes

- The user will manually run `npm publish` when an npm release is needed.
- Do not attempt to publish to npm from the agent by default, because the user account requires OTP and the user handles that step manually.
